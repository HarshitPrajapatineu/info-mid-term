import React from 'react';
import PropTypes from 'prop-types';
import './Feed.scss';

const Feed = () => (
  <div className="Feed">
    Feed Component
  </div>
);

Feed.propTypes = {};

Feed.defaultProps = {};

export default Feed;
